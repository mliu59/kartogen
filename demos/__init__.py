"""Temporary visual demos for ``tectonic_sim`` development.

These scripts are throwaway inspection tools — they exercise a phase's
code paths across several config variations and emit PNGs for visual
review. They're not unit tests (no assertions) and not part of the
public API.

Run any demo with::

    python -m demos.<demo_script_name>

Each writes its output to ``demos/output/<phase>/`` by default.

Again, this space is only used for concept validation. Extensive features should be developed and synced in conjunction with the main module. 
"""
